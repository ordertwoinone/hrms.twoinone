/**
 * Module-local types for Employees. STRUCTURE ONLY.
 *
 * Once the schema exists, derive these from the generated database types so
 * they stay in sync, e.g.:
 *
 *   import type { Database } from "@/types/database.types";
 *   export type Employee = Database["public"]["Tables"]["employees"]["Row"];
 *   export type EmployeeInsert =
 *     Database["public"]["Tables"]["employees"]["Insert"];
 *   export type EmployeeUpdate =
 *     Database["public"]["Tables"]["employees"]["Update"];
 */
export {};
