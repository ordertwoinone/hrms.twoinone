/**
 * Module-local constants for Employees (statuses, employment types, labels).
 * STRUCTURE ONLY — populated when the module is built. Example:
 *
 *   export const EMPLOYMENT_STATUS = {
 *     ACTIVE: "active",
 *     ON_LEAVE: "on_leave",
 *     TERMINATED: "terminated",
 *   } as const;
 */
export {};
